apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: interessemelding
  namespace: default
  labels:
    team: teamtag
spec:
  image: navikt/interessemelding:latest
  team: teamtag
  port: 8080
  ingresses:
    - https://interessemelding.nais.oera-q.local
  liveness:
    path: /isAlive
    initialDelay: 40
  readiness:
    path: /isReady
    initialDelay: 40
